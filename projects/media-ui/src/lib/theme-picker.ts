import { ChangeDetectionStrategy, Component, inject } from '@angular/core';
import { LucideCheck, LucideChevronDown } from '@lucide/angular';
import { MEDIA_UI_THEMES, ThemeService, MediaUiTheme } from './theme.service';

@Component({ selector: 'mm-theme-picker', standalone: true, imports: [LucideCheck, LucideChevronDown], changeDetection: ChangeDetectionStrategy.OnPush, template: `<label class="picker"><span>Theme</span><div class="picker__control"><select [value]="themeService.theme()" (change)="select($event)" aria-label="Choose theme">@for (theme of themes; track theme) { <option [value]="theme">{{ labels[theme] }}</option> }</select><svg lucideChevronDown [size]="16" aria-hidden="true"></svg></div><span class="picker__saved"><svg lucideCheck [size]="13" aria-hidden="true"></svg> saved locally</span></label>`, styles: `.picker { display: grid; gap: 6px; color: var(--mm-component-text-secondary); font: 700 12px/1 var(--mm-font-body); } .picker__control { position: relative; display: flex; align-items: center; } select { appearance: none; width: 190px; padding: 10px 34px 10px 12px; border: 1px solid var(--mm-component-border); border-radius: var(--mm-radius-sm); background: var(--mm-component-control-bg); color: var(--mm-component-text-primary); font: inherit; text-transform: capitalize; } .picker__control [lucideChevronDown] { position: absolute; right: 11px; pointer-events: none; } .picker__saved { display: inline-flex; align-items: center; gap: 4px; color: var(--mm-component-success); font-size: 11px; font-weight: 600; }` })
export class MmThemePicker { readonly themeService = inject(ThemeService); readonly themes = MEDIA_UI_THEMES; readonly labels: Record<MediaUiTheme, string> = { nocturne: 'Nocturne', 'tokyo-night': 'Tokyo Night', 'github-dark-pro': 'GitHub Dark Pro' }; select(event: Event): void { this.themeService.setTheme((event.target as HTMLSelectElement).value as MediaUiTheme); } }
