# Optional GPU transcoding for Jellyfin (requires NVIDIA + nvidia-container-toolkit).
# Usage: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d
services:
  jellyfin:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
